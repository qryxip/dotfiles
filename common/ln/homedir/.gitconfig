[core]
	pager = DELTA_FEATURES=$([ "$COLUMNS" -gt 200 ] && echo side-by-side || echo line-numbers) delta
[interactive]
	diffFilter = delta --color-only
[user]
	name = Ryo Yamashita
	email = qryxip@gmail.com
	singingkey = 72415AFBE13F8207
[commit]
	gpgsign = true
[merge]
	conflictStyle = diff3
[ghq]
	root = ~/src
[delta]
	features = line-numbers decorations
	syntax-theme = Monokai Extended
[mkrepo]
	service = github.com
	username = qryxip
[credential "https://github.com"]
	helper = 
	helper = !/usr/bin/gh auth git-credential
[credential "https://gist.github.com"]
	helper = 
	helper = !/usr/bin/gh auth git-credential
